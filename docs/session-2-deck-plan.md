# 2일차 덱 기획 — 브랜치와 GitHub 첫 PR

> 상태: 기획 초안 (2026-07-07). `slides/session-2.md` 작성의 근거 문서.
> 형제 문서: [course-structure.md](./course-structure.md)

## 전제

- **청중**: 절대 다수가 개발자. 1일차(비개발자 포함 입문)와 달리 용어 풀어쓰기를 줄이고 원리·실무 밀도를 올린다. 단 1일차를 수료한 비개발자도 따라올 수 있게 1일차 비유(나무 가지, 단체사진, 세이브 포인트)는 회수해서 쓴다.
- **선행 사실**: 수강생은 1일차에 **first-contributions 저장소에 기여하며 이미 첫 PR을 보냈다**. 즉 fork → clone → branch → commit → push → PR 전 과정을 "따라 하며" 한 번 경험한 상태다.
- **위치**: 입문 트랙의 완결편. 2일차의 관통 서사는 **"어제 따라 한 모든 동작의 실체를 공개하고, 두 번째 PR은 이해한 채로 스스로 보낸다"**.
- **경계** (중복 방지 원칙):
  - 코드 리뷰 왕복·리뷰 문화 → 3일차 몫. s2는 "PR을 만들어 보내는 것"까지.
  - 포트폴리오 완성(README 다듬기 심화, 프로필, CI 배지) → 4일차 몫. s2는 "기록이 자산"이라는 동기부여와 README 기본기까지.

## 구성안 (6막, 21장 내외)

### 1막 · 다시 잡기 (개발자 모드 전환)

| # | 슬라이드 | 내용 |
|---|---------|------|
| 1 | 타이틀 | 2회차: 브랜치와 GitHub 첫 PR |
| 2 | 1일차 복습 퀴즈 | 3영역 모델·git status 그림 회수. "이 파일은 지금 어디에 있을까요?" 퀴즈식 워밍업 |
| 3 | 어제 만든 PR, 정확히 무슨 일이 일어난 걸까? | 어제 first-contributions에 보낸 **진짜 첫 PR**을 훅으로. fork 버튼, clone 주소, push, Create pull request까지 전부 따라 하긴 했는데 각각 무슨 일이었나? "오늘 덱이 이 질문의 답" 선언 → 4막에서 완전히 해소되는 수미상관 |
| 4 | 오늘의 지도 | 원리 심화 → 실무 워크플로우 → 기록 습관 → 두 번째 PR. 약속: "끝나면 어제 한 모든 동작을 설명할 수 있고, 두 번째 PR은 스스로 보낸다" |

### 2막 · Git 원리 심화

키워드: Git 원리 심화, 버전 관리, reset·revert·stash

| # | 슬라이드 | 내용 |
|---|---------|------|
| 5 | 커밋의 정체 | 스냅샷 + SHA 해시. 해시는 **내용 + 부모 커밋 + 작성자 + 시간 + 메시지**를 반영한 지문(fingerprint) — 1일차 git log의 `a1b2c3d`가 무엇이었는지 공개 |
| 6 | 브랜치의 정체 | **브랜치는 커밋을 가리키는 이름표.** 결론: "브랜치는 공짜다, 마음껏 만들어라" (1일차 '가지' 비유의 실체. 내부 구현 디테일은 다루지 않음) |
| 7 | HEAD의 이동 | checkout이 실제로 하는 일 = HEAD 포인터 옮기기. 다음 장(reset)의 기반 |
| 8 | 되돌리기 1: reset vs revert | HEAD 이동의 응용. reset = 이력을 지움(로컬 전용), revert = 되돌리는 커밋을 새로 쌓음(공유 이력에 안전). 1일차 키워드 "되돌릴 수 있다"가 실제 명령으로 회수되는 지점 |
| 9 | 되돌리기 2: stash | 커밋하기엔 애매한 작업 잠깐 치워두기. 시나리오: 작업 중 급한 브랜치 전환 |

### 3막 · 실무 워크플로우

키워드: branch 전략, conventional commits, 실무 워크플로우

| # | 슬라이드 | 내용 |
|---|---------|------|
| 10 | branch 전략 | GitHub Flow 중심: main은 항상 배포 가능 + 브랜치에서 작업 + PR로 합류. Git Flow는 비교 반 장으로만 (역사적 배경) |
| 11 | conventional commits | `feat:` `fix:` `docs:` `chore:` 타입 문법. 왜 쓰는가: 히스토리 검색, 자동 체인지로그, 팀의 공용어 |
| 12 | 좋은 커밋 단위 | 원자적 커밋. 판별법: 메시지에 "그리고"가 들어가면 쪼개라 |

### 4막 · 협업과 프로세스

키워드: 협업 방법, 프로젝트 관리 프로세스, GitHub 권한 관리, origin·upstream

| # | 슬라이드 | 내용 |
|---|---------|------|
| 13 | 권한이 협업 방식을 가른다 | 저장소 권한(read/write/admin). write 있으면 collaborator(팀, 브랜치 직접 push), 없으면 **fork**(내 사본에서 작업 후 PR). "fork를 왜 하는가"의 답 = 권한. **어제 first-contributions에서 fork 버튼을 눌렀던 이유가 바로 이것** (write 권한이 없었으니까) |
| 14 | origin과 upstream | fork 실습 최대 혼동 지점. **내 fork = origin, 원본 저장소 = upstream, PR = origin의 브랜치를 upstream에 제안하는 것.** 어제 clone한 주소가 origin(내 fork)이었다는 사실 공개. `git remote add upstream`과 upstream에서 최신 받아오기(fetch/pull)까지. 다이어그램 필수 (로컬 ↔ origin ↔ upstream 3자 구도) |
| 15 | GitHub 프로젝트 관리 루프 | Issue → branch → commit → PR → merge. 이슈 번호(`#12`)로 연결되는 추적성. 실무의 일감 관리가 이 루프 위에서 돈다 |
| 16 | PR이라는 문서 | 제목·설명·연결된 이슈. 리뷰어가 읽는 순서로 쓰기. **1막의 훅 질문("어제 만든 PR은 무슨 일이었나")이 여기서 완전히 풀린다.** (리뷰 왕복 자체는 3일차 예고) |

### 5막 · 기록이 자산이 된다 (축소: 4일차와 겹침 방지)

키워드: README.md 문서 작성, 깃허브에 기록, 취업 포트폴리오

| # | 슬라이드 | 내용 |
|---|---------|------|
| 17 | README.md | 프로젝트의 얼굴. 필수 섹션: 무엇을/왜/설치/사용법. 기본기까지만 (심화·꾸미기는 4일차) |
| 18 | 깃허브에 기록 = 공개 이력서 | 잔디, 커밋 메시지, PR 설명을 채용담당자가 실제로 본다. conventional commits 회수. 말미에 로드맵 한 줄: 이 저장소가 3일차(CI 배지) → 4일차(포트폴리오)로 자란다 |

### 6막 · 실전 미션

키워드: git-GitHub 인증

| # | 슬라이드 | 내용 |
|---|---------|------|
| 19 | 미션 준비: Git과 GitHub 연결 | 인증 2가지: HTTPS(PAT 토큰 + credential helper로 저장) vs SSH key(키 등록 한 번, 이후 무비밀번호). 선택 기준과 설정 흐름. 첫 push에서 전원이 막히는 지점이라 미션 직전 배치 |
| 20 | 미션 안내 | **두 번째 PR: 이번엔 이해하고 보낸다.** fork → clone → branch → conventional commit → push → PR을 안내 없이 스스로 (origin → upstream 구도를 알고서). 어제와의 차이를 스스로 체감하는 게 목표. course.json s2 미션(수동 체크)과 연동 |
| 21 | 마무리 | 오늘 남긴 기록 정리 + 3일차 예고: "이 PR에 내일 CI와 리뷰가 붙습니다" |

## 키워드 커버리지

| 키워드 | 슬라이드 |
|--------|---------|
| Git 원리 심화 | 5, 6, 7 |
| branch 전략 | 10 |
| conventional commits | 11, 12, (18 회수) |
| 프로젝트 관리 프로세스 | 15 |
| 협업 방법 | 13, 14, 16 |
| 버전 관리 | 5~9 (원리·되돌리기 전반) |
| README.md 문서 작성 | 17 |
| 취업 포트폴리오 | 18 (로드맵 프리뷰) |
| 실무 워크플로우 | 10~16 (막 전체) |
| 깃허브에 기록 | 18, 20 |
| GitHub 권한 관리 (fork 이유) | 13 |
| origin · upstream | 14, 20 |
| reset · revert · stash | 8, 9 |
| git-GitHub 인증 | 19 |

## s1 자산 재사용

- 3영역 모델 다이어그램(단체사진 슬라이드), git status 흐름도 → 복습 퀴즈(#2)에서 재사용
- gitGraph 다크 themeVariables·edge 라벨 themeCSS → 원리·전략 다이어그램에 그대로
- 배경: `public/s2/background-s2.png` (deckBackground 배선 완료)
- 대본은 처음부터 큐 카드 형식으로 작성 ([click] 동기화, Q:/→ 표기)
- 슬라이드 규칙: 제목 간결+부제, em dash 금지, 한쪽 면 색칠 금지, 흰색+amber

## 미해결 질문

- [ ] 커리큘럼 표의 2일차 문구("브랜치와 GitHub 첫 PR") 재검토: 첫 PR은 1일차(first-contributions)에서 이미 달성 → "Git 심화와 두 번째 PR", "이해하고 보내는 PR" 등 사실에 맞는 명칭 필요. s1 덱 마지막 장의 "내일 첫 PR" 예고 문구도 함께 정리
- [ ] 실습 플랫폼: 브라우저 터미널만으로 갈지, 이날부터 실제 로컬 Git + GitHub 계정으로 전환할지 (인증 슬라이드는 로컬 전환을 전제로 함. 단 1일차에 first-contributions를 했다면 계정·기초 환경은 이미 있음)
- [ ] 두 번째 PR의 대상 저장소: first-contributions는 명단 추가 1회성이라 재사용 부적합 → 강의용 연습 저장소 신설 필요성 커짐
- [ ] reset --hard의 위험 고지 수위 (입문자 보호 vs 실무 현실)
